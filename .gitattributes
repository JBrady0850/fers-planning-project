# Normalize line endings in the repository to LF.
* text=auto eol=lf

# PowerShell is happier with CRLF in the working copy on Windows.
*.ps1 text eol=crlf

# The shell script must keep LF or it will not run.
*.sh text eol=lf

# Binary.
*.pdf binary
